#include <bits/stdc++.h>

using namespace std;

int main()

{
    long long l, r, result = 0;
    cin >> l >> r;
    if(l == r)
        cout << l << endl;
    else
        cout << 2 << endl;
    return 0;
}